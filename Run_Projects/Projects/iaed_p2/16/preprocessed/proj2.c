/*File generated by PreProcessor.py*/


#include "principal.h"
#include "codigoAux.h"
#include "jogo.h"
#include "listaJogo.h"
#include "hashtableJogo.h"
#include "equipa.h"
#include "listaEquipa.h"
#include "hashtableEquipa.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>


int main()
{
  listaJogo **headsJogos = 0;
  int M_J = 1021;
  listaJogo *listaDeJogos = 0;
  listaEquipa **headsEquipas = 0;
  int M_E = 1021;
  listaEquipa *listaDeEquipas = 0;
  char comando[4000];
  int NL = 1;
  int *pNL = &NL;
  headsJogos = criaHashTableJogo(M_J);
  listaDeJogos = criaListaJogo();
  headsEquipas = criaHashTableEquipa(M_E);
  listaDeEquipas = criaListaEquipa();
  while ((1 && (strcmp(comando, "x") != 0)) && (strcmp(comando, "X") != 0))
  {
    for (int comando_index = 0; comando_index < 10; comando_index++)
    {
      comando[comando_index] = new_sym_var(sizeof(char) * 8);
    }

    comando[10 - 1] = '\0';
    analisaComando(comando, headsJogos, headsEquipas, listaDeJogos, listaDeEquipas, M_J, M_E, pNL);
    *pNL += 1;
  }

  freeHashTableJogo(headsJogos, M_J);
  freeListaMaisJogos(listaDeJogos);
  freeHashTableEquipa(headsEquipas, M_E);
  freeListaMaisEquipas(listaDeEquipas);
  return 0;
}

