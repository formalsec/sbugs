/*File generated by PreProcessor.py*/


#include "team.h"
#include "messages.h"
#include <stdlib.h>
#include <stdio.h>
#include <string.h>


Team *createTeam(char *name)
{
  Team *team = (Team *) malloc(sizeof(Team));
  int length = strlen(name);
  team->name = (char *) malloc((sizeof(char)) * (length + 1));
  strcpy(team->name, name);
  team->victories = 0;
  return team;
}

void deleteTeam(void *team)
{
  if (team != 0)
  {
    free(((Team *) team)->name);
    free(team);
  }
  else
  {
    
  }

}

void updateVictories(Team *team, int newVictories)
{
  if (team != 0)
  {
    team->victories += newVictories;
  }
  else
  {
    
  }

}

int compareTeamFunction(char *name, void *team)
{
  if ((team != 0) && (name != 0))
  {
    if (strcmp(name, ((Team *) team)->name) == 0)
    {
      return 1;
    }
    else
    {
      
    }

  }
  else
  {
    
  }

  return 0;
}

void printTeam(void *team, int *cmdCounter)
{
  if (team != 0)
  {
    printf("%d %s %d\n", *cmdCounter, ((Team *) team)->name, ((Team *) team)->victories);
  }
  else
  {
    
  }

}

void bestTeamsFunction(void *team, int *cmdCounter)
{
  printf("%d * %s\n", *cmdCounter, ((Team *) team)->name);
}

