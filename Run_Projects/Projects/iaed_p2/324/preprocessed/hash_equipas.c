/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "projeto2.h"


void insert_new_e_node(e_node **head, char *nome_equipa)
{
  e_node *new_node = malloc(sizeof(e_node));
  e_node *current;
  new_node->nome = malloc((sizeof(char)) * (strlen(nome_equipa) + 1));
  strcpy(new_node->nome, nome_equipa);
  new_node->vitorias = 0;
  new_node->next = 0;
  if ((*head) == 0)
  {
    {
      *head = new_node;
    }
  }
  else
  {
    {
      current = *head;
      while (current->next != 0)
      {
        current = current->next;
      }

      current->next = new_node;
    }
  }

}

void insert_into_equipas(e_node **hashtable, char *nome)
{
  e_node *head;
  int location = get_location(nome);
  if (hashtable[location] == 0)
  {
    {
      head = 0;
      insert_new_e_node(&head, nome);
      hashtable[location] = head;
    }
  }
  else
  {
    {
      head = hashtable[location];
      insert_new_e_node(&head, nome);
      hashtable[location] = head;
    }
  }

}

