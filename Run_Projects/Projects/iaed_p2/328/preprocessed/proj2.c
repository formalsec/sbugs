/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "commands.h"


int main()
{
  char command;
  int NL = 1;
  h_t_ordered_list *lst = init_list();
  list_names *head_lst_names = 0;
  hash_table_teams *team_ht;
  hash_table_games *game_ht;
  team_ht = create_ht_team(1051);
  game_ht = create_ht_game(1051);
  while ((command = getchar()) != EOF)
  {
    switch (command)
    {
      default:
        break;

      case 'a':
        new_game(game_ht, team_ht, lst, NL);
        NL++;
        break;

      case 'l':
        list_all_games(lst, NL);
        NL++;
        break;

      case 'p':
        search_game(game_ht, NL);
        NL++;
        break;

      case 'r':
        delete_game(game_ht, team_ht, lst, NL);
        NL++;
        break;

      case 's':
        change_score(game_ht, team_ht, NL);
        NL++;
        break;

      case 'A':
        new_team(team_ht, NL);
        NL++;
        break;

      case 'P':
        search_team(team_ht, NL);
        NL++;
        break;

      case 'g':
        find_winners(team_ht, head_lst_names, NL);
        NL++;
        break;

      case 'x':
        command_x(game_ht, team_ht, lst);
        return 0;

    }

  }

  return 0;
}

