/*File generated by PreProcessor.py*/


#include <string.h>
#include <stdlib.h>
#include "Game.h"


Game *newGame(char *name, Team *team1, Team *team2, int score1, int score2)
{
  Game *pnew_game = (Game *) malloc(sizeof(Game));
  pnew_game->pname = (char *) malloc((sizeof(char)) * (strlen(name) + 1));
  strcpy(pnew_game->pname, name);
  pnew_game->pteam1 = team1;
  pnew_game->pteam2 = team2;
  pnew_game->score1 = score1;
  pnew_game->score2 = score2;
  return pnew_game;
}

void delGame(Game *game)
{
  free(game->pname);
  free(game);
}

Team *who_won(Game *ptr_g)
{
  if (ptr_g->score1 > ptr_g->score2)
  {
    return ptr_g->pteam1;
  }
  else
  {
    if (ptr_g->score1 < ptr_g->score2)
    {
      return ptr_g->pteam2;
    }
    else
    {
      return 0;
    }

  }

}

