#ifndef AUX_H
#define AUX_H

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

/*Prototipos de funcoes*/

char* strdup(const char*s);

unsigned long hash_chave(int M, char *chave);


#endif
