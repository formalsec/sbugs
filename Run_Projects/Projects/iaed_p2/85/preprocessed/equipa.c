/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "aux.h"
#include "equipa.h"


pEquipa cria_equipa(char *n_eq)
{
  pEquipa eq;
  eq = malloc(sizeof(struct equipa));
  eq->n_eq = strdup(n_eq);
  eq->wins = 0;
  return eq;
}

void print_eq(pEquipa eq)
{
  printf("%s %d\n", eq->n_eq, eq->wins);
}

void free_eq(pEquipa eq)
{
  free(eq->n_eq);
  free(eq);
}

