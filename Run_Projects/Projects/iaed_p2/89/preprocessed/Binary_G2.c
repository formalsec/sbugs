/*File generated by PreProcessor.py*/


#include "Binary_G2.h"


BGlink BGinit()
{
  return 0;
}

BGlink BGnew(Game game)
{
  BGlink new = (BGlink) malloc(sizeof(struct BGnode));
  new->game = game;
  new->l = 0;
  new->r = 0;
  return new;
}

BGlink BGmax(BGlink link)
{
  if ((link == 0) || (link->r == 0))
  {
    return link;
  }
  else
  {
    return BGmax(link->r);
  }

}

BGlink BGinsert(BGlink link, Game game)
{
  if (link == 0)
  {
    return BGnew(game);
  }
  else
  {
    
  }

  if (strcmp(game->name, BGgetgame(link)->name) < 0)
  {
    link->l = BGinsert(link->l, game);
  }
  else
  {
    link->r = BGinsert(link->r, game);
  }

  return link;
}

Game BGsearch(BGlink link, char *name)
{
  if (link == 0)
  {
    return 0;
  }
  else
  {
    
  }

  if (strcmp(BGgetgame(link)->name, name) == 0)
  {
    return BGgetgame(link);
  }
  else
  {
    
  }

  if (strcmp(name, BGgetgame(link)->name) < 0)
  {
    return BGsearch(link->l, name);
  }
  else
  {
    return BGsearch(link->r, name);
  }

}

BGlink BGdelete(BGlink link, Game game)
{
  BGlink aux;
  Game game_aux;
  if (link == 0)
  {
    return link;
  }
  else
  {
    if (strcmp(game->name, BGgetgame(link)->name) < 0)
    {
      link->l = BGdelete(link->l, game);
    }
    else
    {
      if (strcmp(BGgetgame(link)->name, game->name) < 0)
      {
        link->r = BGdelete(link->r, game);
      }
      else
      {
        {
          if ((link->l != 0) && (link->r != 0))
          {
            {
              aux = BGmax(link->l);
              game_aux = BGgetgame(link);
              link->game = BGgetgame(aux);
              aux->game = game_aux;
              link->l = BGdelete(link->l, BGgetgame(aux));
            }
          }
          else
          {
            {
              aux = link;
              if ((link->l == 0) && (link->r == 0))
              {
                link = 0;
              }
              else
              {
                if (link->l == 0)
                {
                  link = link->r;
                }
                else
                {
                  link = link->l;
                }

              }

              free(aux);
            }
          }

        }
      }

    }

  }

  return link;
}

int Traverse(BGlink link, Game *game, int i)
{
  if (link == 0)
  {
    return i;
  }
  else
  {
    
  }

  game[i] = BGgetgame(link);
  i++;
  i = Traverse(link->l, game, i);
  i = Traverse(link->r, game, i);
  return i;
}

Game BGgetgame(BGlink link)
{
  return link->game;
}

