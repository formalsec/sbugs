/*File generated by PreProcessor.py*/


#include "tabela_jogo.h"


int hashTabJogos(char *s)
{
  int h = 0;
  int a = 127;
  for (; (*s) != '\0'; s++)
    h = ((a * h) + (*s)) % 499;

  return h;
}

TabJogos *novaTabJogos()
{
  int i;
  TabJogos *tab = malloc(sizeof(TabJogos));
  tab->lst = malloc(499 * (sizeof(LstJogos *)));
  tab->n_jogos = 0;
  for (i = 0; i < 499; i++)
    tab->lst[i] = novaListaJogos();

  return tab;
}

void pushTabJogos(TabJogos *tab, Jogo j)
{
  int h;
  h = hashTabJogos(j.nome);
  pushJogo(j, tab->lst[h]);
  tab->n_jogos++;
}

void popTabJogos(TabJogos *tab, char *nome)
{
  int h;
  h = hashTabJogos(nome);
  removeJogo(tab->lst[h], nome);
  tab->n_jogos--;
}

Jogo *procuraTabJogos(TabJogos *tab, char *nome)
{
  int h;
  Jogo *j;
  h = hashTabJogos(nome);
  j = procuraLstJogos(tab->lst[h], nome);
  return j;
}

Jogo *copiaTabJogos(TabJogos *tab)
{
  int i;
  int j;
  int k = 0;
  Jogo **arr = malloc(499 * (sizeof(Jogo *)));
  Jogo *res = malloc(tab->n_jogos * (sizeof(Jogo)));
  for (i = 0; i < 499; i++)
    arr[i] = copiaLstJogos(tab->lst[i]);

  for (i = 0; i < 499; i++)
    for (j = 0; j < tab->lst[i]->n_jogos; j++)
    res[k++] = arr[i][j];


  for (i = 0; i < 499; i++)
    free(arr[i]);

  free(arr);
  return res;
}

void destroiTabJogos(TabJogos *tab)
{
  int i;
  for (i = 0; i < 499; i++)
    destroiLstJogos(tab->lst[i]);

  free(tab->lst);
  free(tab);
}

int comparaJogos(const void *a, const void *b)
{
  Jogo *jogoA = (Jogo *) a;
  Jogo *jogoB = (Jogo *) b;
  if (jogoA->id > jogoB->id)
  {
    return 1;
  }
  else
  {
    if (jogoA->id < jogoB->id)
    {
      return -1;
    }
    else
    {
      
    }

  }

  return 0;
}

