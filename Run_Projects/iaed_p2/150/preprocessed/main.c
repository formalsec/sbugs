/*File generated by PreProcessor.py*/


#include "proj.h"
#include <stdio.h>


int main()
{
  char command;
  char name[1024];
  char team1[1024];
  char team2[1024];
  int score1;
  int score2;
  int NewLine = 0;
  int nGames = -1;
  int indexGames = 0;
  int *countInput = &NewLine;
  int *countGames = &nGames;
  int *index = &indexGames;
  TEAMlink *Teams;
  GAMElink *Games;
  Games = GAMEinit();
  Teams = TEAMinit();
  while ((command = getchar()) != 'x')
  {
    switch (command)
    {
      case 'a':
        for (int name_index = 0; name_index < 10; name_index++)
      {
        name[name_index] = new_sym_var(sizeof(char) * 8);
      }

        name[10 - 1] = '\0';
        for (int team1_index = 0; team1_index < 10; team1_index++)
      {
        team1[team1_index] = new_sym_var(sizeof(char) * 8);
      }

        team1[10 - 1] = '\0';
        for (int team2_index = 0; team2_index < 10; team2_index++)
      {
        team2[team2_index] = new_sym_var(sizeof(char) * 8);
      }

        team2[10 - 1] = '\0';
        score1 = new_sym_var(sizeof(int) * 8);
        score2 = new_sym_var(sizeof(int) * 8);
        AddNewGame(name, team1, team2, score1, score2, Teams, Games, countInput, countGames, index);
        break;

      case 'A':
        for (int name_index = 0; name_index < 10; name_index++)
      {
        name[name_index] = new_sym_var(sizeof(char) * 8);
      }

        name[10 - 1] = '\0';
        AddNewTeam(name, Teams, countInput);
        break;

      case 'p':
        for (int name_index = 0; name_index < 10; name_index++)
      {
        name[name_index] = new_sym_var(sizeof(char) * 8);
      }

        name[10 - 1] = '\0';
        SearchGame(name, Games, countInput);
        break;

      case 'r':
        for (int name_index = 0; name_index < 10; name_index++)
      {
        name[name_index] = new_sym_var(sizeof(char) * 8);
      }

        name[10 - 1] = '\0';
        DeleteGame(name, Teams, Games, countInput, countGames);
        break;

      case 's':
        for (int name_index = 0; name_index < 10; name_index++)
      {
        name[name_index] = new_sym_var(sizeof(char) * 8);
      }

        name[10 - 1] = '\0';
        score1 = new_sym_var(sizeof(int) * 8);
        score2 = new_sym_var(sizeof(int) * 8);
        ChangeScore(name, score1, score2, Teams, Games, countInput);
        break;

      case 'P':
        for (int name_index = 0; name_index < 10; name_index++)
      {
        name[name_index] = new_sym_var(sizeof(char) * 8);
      }

        name[10 - 1] = '\0';
        SearchTeam(name, Teams, countInput);
        break;

      case 'l':
        ListsGames(Games, countGames, countInput);
        break;

      case 'g':
        ListsBestTeams(Teams, countInput);
        break;

    }

  }

  TEAMSfree(Teams);
  GAMESfree(Games);
  return 0;
}

