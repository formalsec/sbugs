/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "hash_table_equipa.h"
#include "comandos.h"


equipa *init_hash_table_equipa(equipa *hash[2000])
{
  int i;
  for (i = 0; i < 2000; i++)
  {
    hash[i] = 0;
  }

  return *hash;
}

equipa *hash_table_equipa_insert(equipa *e, equipa *hash[2000])
{
  int indice;
  indice = hash_function(e->nome_equipa);
  e->next = hash[indice];
  hash[indice] = e;
  return *hash;
}

equipa *hash_table_equipa_lookup(char *nome, equipa *hash[2000])
{
  int indice;
  equipa *aux;
  indice = hash_function(nome);
  for (aux = hash[indice]; aux != 0; aux = aux->next)
  {
    if (strcmp(aux->nome_equipa, nome) == 0)
    {
      {
        return aux;
      }
    }
    else
    {
      
    }

  }

  return 0;
}

equipa *hash_table_equipa_delete(char *nome, equipa *hash[2000])
{
  int indice;
  equipa *aux;
  equipa *aux_ant;
  indice = hash_function(nome);
  aux = hash[indice];
  aux_ant = 0;
  while ((aux != 0) && (strcmp(aux->nome_equipa, nome) != 0))
  {
    aux_ant = aux;
    aux = aux->next;
  }

  if (aux == 0)
  {
    {
      return 0;
    }
  }
  else
  {
    
  }

  if (aux_ant == 0)
  {
    {
      hash[indice] = aux->next;
    }
  }
  else
  {
    {
      aux_ant->next = aux->next;
    }
  }

  return *hash;
}

