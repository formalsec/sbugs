/*File generated by PreProcessor.py*/


#include "data.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>


char *NewString(char *string)
{
  int size = strlen(string) + 1;
  char *string_adress = malloc(size);
  string_adress = (char *) memcpy(string_adress, string, size);
  return string_adress;
}

Team NewTeam(char *name)
{
  Team team = (Team) malloc(sizeof(dteam));
  team->name = NewString(name);
  team->wins = 0;
  return team;
}

void DeleteTeam(Team team)
{
  free(team->name);
  free(team);
}

char *NameTeam(Team team)
{
  return team->name;
}

int Wins(Team team)
{
  return team->wins;
}

void ChangeWins(Team team, int n)
{
  if ((team->wins > 0) || (n > 0))
  {
    {
      team->wins += n;
    }
  }
  else
  {
    
  }

}

Game NewGame(char *name, char *team1, char *team2, int score1, int score2)
{
  Game game = (Game) malloc(sizeof(dgame));
  game->name = NewString(name);
  game->team1 = NewString(team1);
  game->team2 = NewString(team2);
  game->score[0] = score1;
  game->score[1] = score2;
  return game;
}

void DeleteGame(Game game)
{
  free(game->name);
  free(game->team1);
  free(game->team2);
  free(game);
}

char *NameGame(Game game)
{
  return game->name;
}

char *Team1(Game game)
{
  return game->team1;
}

char *Team2(Game game)
{
  return game->team2;
}

int Score1(Game game)
{
  return game->score[0];
}

int Score2(Game game)
{
  return game->score[1];
}

void ChangeScore(Game game, int score1, int score2)
{
  game->score[0] = score1;
  game->score[1] = score2;
}

