/*File generated by PreProcessor.py*/


#include "auxiliary.h"


char *strdupl(const char *str)
{
  size_t size = strlen(str) + 1;
  char *cpy = malloc(size);
  if (cpy)
  {
    memcpy(cpy, str, size);
  }
  else
  {
    
  }

  return cpy;
}

int stringhash(char *name)
{
  int i;
  int key = 0;
  for (i = 0; name[i] != '\0'; i++)
    key = ((key * 857) + name[i]) % 419;

  return key;
}

