/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "equipas.h"
#include "auxiliares.h"


void freeE(linkE *head)
{
  linkE aux1;
  linkE aux2;
  for (aux1 = *head; aux1 != 0;)
  {
    free(aux1->nome_equipa);
    aux2 = aux1;
    aux1 = aux1->next;
    free(aux2);
  }

}

linkE NOVO_E(char *nome)
{
  linkE nova_e = (linkE) malloc(sizeof(struct node));
  nova_e->nome_equipa = (char *) malloc((sizeof(char)) * (strlen(nome) + 1));
  strcpy(nova_e->nome_equipa, nome);
  nova_e->vitorias = 0;
  nova_e->next = 0;
  return nova_e;
}

void insereInicio_E(linkE *headptr, char *nome)
{
  linkE nova_e = NOVO_E(nome);
  nova_e->next = *headptr;
  *headptr = nova_e;
}

linkE encontra_E(linkE head, char *nome)
{
  linkE objetivo;
  for (objetivo = head; objetivo != 0; objetivo = objetivo->next)
    if (strcmp(objetivo->nome_equipa, nome) == 0)
  {
    return objetivo;
  }
  else
  {
    
  }


  return 0;
}

