/*File generated by PreProcessor.py*/


#include <stdlib.h>
#include <string.h>
#include "Game.h"


Game *newGame(unsigned int id, unsigned int score[], Team *team1, Team *team2, char *name)
{
  Game *new;
  char *n;
  new = (Game *) malloc(sizeof(Game));
  new->id = id;
  new->score[0] = score[0];
  new->score[1] = score[1];
  new->team1 = team1;
  new->team2 = team2;
  new->isDeleted = 0;
  n = (char *) malloc((sizeof(char)) * (strlen(name) + 1));
  strcpy(n, name);
  new->name = n;
  if (score[0] > score[1])
  {
    {
      new->team1->nWon++;
    }
  }
  else
  {
    if (score[0] < score[1])
    {
      {
        new->team2->nWon++;
      }
    }
    else
    {
      
    }

  }

  return new;
}

void freeGame(Game *game)
{
  free(game->name);
  free(game);
}

