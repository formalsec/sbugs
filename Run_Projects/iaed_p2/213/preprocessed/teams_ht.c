/*File generated by PreProcessor.py*/


#include <stdlib.h>
#include "teams_ht.h"


Team *T_init()
{
  int i;
  Team *teams = malloc(48611 * (sizeof(Team)));
  for (i = 0; i < 48611; i++)
    teams[i] = 0;

  return teams;
}

void T_insert(Team *teams, Team team)
{
  int i = hash(team->name, 48611);
  while (teams[i])
    i = (i + 1) % 48611;

  teams[i] = team;
}

Team T_search(Team *teams, char *name)
{
  int i = hash(name, 48611);
  while (teams[i])
  {
    if (strcmp(teams[i]->name, name) == 0)
    {
      return teams[i];
    }
    else
    {
      i = (i + 1) % 48611;
    }

  }

  return 0;
}

int find_winner(Team *teams, int *num)
{
  int i;
  int max = -1;
  *num = 0;
  for (i = 0; i < 48611; i++)
  {
    if (teams[i])
    {
      {
        if (teams[i]->wins > max)
        {
          {
            max = teams[i]->wins;
            *num = 1;
          }
        }
        else
        {
          if (teams[i]->wins == max)
          {
            (*num)++;
          }
          else
          {
            
          }

        }

      }
    }
    else
    {
      
    }

  }

  return max;
}

void get_winners(Team *teams, Team *winners, int max)
{
  int i;
  int j = 0;
  for (i = 0; i < 48611; i++)
    if (teams[i] && (teams[i]->wins == max))
  {
    winners[j++] = teams[i];
  }
  else
  {
    
  }


}

void T_destroy(Team *teams)
{
  int i;
  for (i = 0; i < 48611; i++)
    if (teams[i])
  {
    freeTeam(teams[i]);
  }
  else
  {
    
  }


  free(teams);
}

