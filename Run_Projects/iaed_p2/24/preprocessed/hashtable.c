/*File generated by PreProcessor.py*/


#include "hashtable.h"


int hashkey(char *key)
{
  unsigned long hash = 0;
  int a = 127;
  for (; (*key) != '\0'; key++)
  {
    hash = ((a * hash) + (*key)) % 503;
  }

  return hash;
}

Hash initHash()
{
  int i;
  Hash hashtable = (Hash) malloc((sizeof(Link)) * 503);
  for (i = 0; i < 503; i++)
  {
    hashtable[i] = 0;
  }

  return hashtable;
}

void insertHash(Hash hashtable, char *key, Link new)
{
  int i = hashkey(key);
  insertLL(&hashtable[i], new);
}

Link searchHash(Hash hashtable, char *key)
{
  int i = hashkey(key);
  return searchLL(hashtable[i], key);
}

void removeHash(Hash hashtable, char *key)
{
  int i = hashkey(key);
  removeLL(&hashtable[i], key);
}

