/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "projeto2.h"


void insert_new_node(node **head, jg_node *jg)
{
  node *new_node = malloc(sizeof(node));
  node *current;
  new_node->nome_jogo = malloc((sizeof(char)) * (strlen(jg->value) + 1));
  strcpy(new_node->nome_jogo, jg->value);
  new_node->jogo = jg;
  new_node->next = 0;
  if ((*head) == 0)
  {
    {
      *head = new_node;
    }
  }
  else
  {
    {
      current = *head;
      while (current->next != 0)
      {
        current = current->next;
      }

      current->next = new_node;
    }
  }

}

void insert_into_table(node **hashtable, jg_node *jg)
{
  node *head;
  int location = get_location(jg->value);
  if (hashtable[location] == 0)
  {
    {
      head = 0;
      insert_new_node(&head, jg);
      hashtable[location] = head;
    }
  }
  else
  {
    {
      head = hashtable[location];
      insert_new_node(&head, jg);
      hashtable[location] = head;
    }
  }

}

