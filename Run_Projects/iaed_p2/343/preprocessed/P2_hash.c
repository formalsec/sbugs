/*File generated by PreProcessor.py*/


#include "hash.h"


int f_hash(char *des)
{
  int res = 0;
  int a = 53;
  for (; (*des) != '\0'; des++)
    res = ((a * res) + (*des)) % 157;

  return res;
}

node_eq *init_eq()
{
  int i;
  node_eq *table_eq;
  table_eq = malloc((sizeof(node_eq)) * 157);
  for (i = 0; i < 157; i++)
    table_eq[i] = 0;

  return table_eq;
}

node_jg *init_jg()
{
  int i;
  node_jg *table_jg;
  table_jg = malloc((sizeof(node_jg)) * 157);
  for (i = 0; i < 157; i++)
    table_jg[i] = 0;

  return table_jg;
}

link *init_lk()
{
  link *nlink;
  nlink = malloc(sizeof(link));
  nlink->head = 0;
  nlink->tail = 0;
  return nlink;
}

void trc_eq(int i, int j, eq **lista)
{
  eq *equipa;
  equipa = lista[i];
  lista[i] = lista[j];
  lista[j] = equipa;
}

int div_eq(eq **lista, int e, int d)
{
  int i = e - 1;
  int j = d;
  eq *equipa = lista[d];
  while (i < j)
  {
    while (strcmp(lista[++i]->des, equipa->des) < 0)
      ;

    while (strcmp(equipa->des, lista[--j]->des) < 0)
      if (j == e)
    {
      break;
    }
    else
    {
      
    }


    if (i < j)
    {
      trc_eq(i, j, lista);
    }
    else
    {
      
    }

  }

  trc_eq(i, d, lista);
  return i;
}

void quicksort(eq **lista, int e, int d)
{
  int i;
  if (d <= e)
  {
    return;
  }
  else
  {
    
  }

  i = div_eq(lista, e, d);
  quicksort(lista, e, i - 1);
  quicksort(lista, i + 1, d);
}

