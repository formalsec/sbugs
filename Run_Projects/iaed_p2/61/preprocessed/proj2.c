/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <stdlib.h>
#include "futebol.h"


int main()
{
  int comando;
  int sc1;
  int sc2;
  int NL;
  char nome[1024];
  char eq1[1024];
  char eq2[1024];
  hashtable *hequipas;
  hashtable *hjogos;
  list *ljogos;
  NL = 0;
  hequipas = criahash(1049);
  hjogos = criahash(1049);
  ljogos = crialista();
  while ((comando = getchar()) != 'x')
  {
    switch (comando)
    {
      case 'a':
        NL++;
        for (int nome_index = 0; nome_index < 10; nome_index++)
      {
        nome[nome_index] = new_sym_var(sizeof(char) * 8);
      }

        nome[10 - 1] = '\0';
        for (int eq1_index = 0; eq1_index < 10; eq1_index++)
      {
        eq1[eq1_index] = new_sym_var(sizeof(char) * 8);
      }

        eq1[10 - 1] = '\0';
        for (int eq2_index = 0; eq2_index < 10; eq2_index++)
      {
        eq2[eq2_index] = new_sym_var(sizeof(char) * 8);
      }

        eq2[10 - 1] = '\0';
        sc1 = new_sym_var(sizeof(int) * 8);
        sc2 = new_sym_var(sizeof(int) * 8);
        adiciona_jogo(nome, eq1, eq2, sc1, sc2, hequipas, hjogos, ljogos, NL);
        break;

      case 'l':
        NL++;
        lista_jogos(ljogos, NL);
        break;

      case 'p':
        NL++;
        for (int nome_index = 0; nome_index < 10; nome_index++)
      {
        nome[nome_index] = new_sym_var(sizeof(char) * 8);
      }

        nome[10 - 1] = '\0';
        procura_jogo(hjogos, nome, NL);
        break;

      case 'r':
        NL++;
        for (int nome_index = 0; nome_index < 10; nome_index++)
      {
        nome[nome_index] = new_sym_var(sizeof(char) * 8);
      }

        nome[10 - 1] = '\0';
        apaga_jogo(hjogos, ljogos, nome, NL);
        break;

      case 's':
        NL++;
        for (int nome_index = 0; nome_index < 10; nome_index++)
      {
        nome[nome_index] = new_sym_var(sizeof(char) * 8);
      }

        nome[10 - 1] = '\0';
        sc1 = new_sym_var(sizeof(int) * 8);
        sc2 = new_sym_var(sizeof(int) * 8);
        altera_score(hjogos, nome, sc1, sc2, NL);
        break;

      case 'A':
        NL++;
        for (int nome_index = 0; nome_index < 10; nome_index++)
      {
        nome[nome_index] = new_sym_var(sizeof(char) * 8);
      }

        nome[10 - 1] = '\0';
        adiciona_equipa(nome, hequipas, NL);
        break;

      case 'P':
        NL++;
        for (int nome_index = 0; nome_index < 10; nome_index++)
      {
        nome[nome_index] = new_sym_var(sizeof(char) * 8);
      }

        nome[10 - 1] = '\0';
        procura_equipa(hequipas, nome, NL);
        break;

      case 'g':
        NL++;
        mais_vitorias(hequipas, 1049, NL);
        break;

      default:
        printf("ERRO: comando desconhecido.\n");

    }

    getchar();
  }

  freelista(ljogos, 0, free);
  freehash(hequipas, 1049, 1, freeequipa);
  freehash(hjogos, 1049, 1, freejogo);
  return 0;
}

