/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <stdlib.h>


typedef struct 
{
  int posicao;
  int numfilhos;
  int nivel;
  int *nodulos;
} Node;
typedef struct 
{
  int valor;
  int numpais;
} Node_Aux;
Node *tabela;
Node_Aux *vetor;
int numpieces;
int dependecia;
int min_interacao = 0;
int maior_caminho = 0;
void colocaposicao(int dependente, int pessa)
{
  if (tabela[pessa].numfilhos > 0)
  {
    tabela[pessa].numfilhos++;
    tabela[pessa].nodulos = realloc(tabela[pessa].nodulos, (sizeof(int)) * tabela[pessa].numfilhos);
    tabela[pessa].nodulos[tabela[pessa].numfilhos - 1] = dependente;
  }
  else
  {
    if (tabela[pessa].numfilhos == 0)
    {
      tabela[pessa].nodulos = malloc(sizeof(int));
      tabela[pessa].nodulos[0] = dependente;
      tabela[pessa].numfilhos = 1;
    }
    else
    {
      exit(1);
    }

  }

}

void checkpais(int pessa)
{
  int i = 0;
  for (i = 0; i < tabela[pessa].numfilhos; i++)
  {
    vetor[tabela[pessa].nodulos[i]].numpais++;
  }

}

void interacoes()
{
  int i = 1;
  for (i = 1; i < numpieces; i++)
  {
    if (vetor[i].numpais == 0)
    {
      min_interacao++;
    }
    else
    {
      
    }

  }

}

void caminho(int pai)
{
  if (tabela[pai].numfilhos == 0)
  {
    if (tabela[pai].nivel > maior_caminho)
    {
      maior_caminho = tabela[pai].nivel;
    }
    else
    {
      
    }

  }
  else
  {
    int i = 0;
    for (i = 0; i < tabela[pai].numfilhos; i++)
    {
      if (tabela[tabela[pai].nodulos[i]].nivel < (tabela[pai].nivel + 1))
      {
        tabela[tabela[pai].nodulos[i]].nivel = tabela[pai].nivel + 1;
        caminho(tabela[pai].nodulos[i]);
      }
      else
      {
        
      }

    }

  }

}

int main()
{
  int pessa;
  int dependente;
  int i = 0;
  int j = 0;
  int k = 0;
  int l = 1;
  int m = 1;
  if (2 == 2)
  {
    numpieces = new_sym_var(sizeof(int) * 8);
    dependecia = new_sym_var(sizeof(int) * 8);
    if ((numpieces == 0) && (dependecia == 0))
    {
      printf("0 0\n");
      return 0;
    }
    else
    {
      if ((numpieces == 1) && (dependecia == 0))
      {
        printf("1 1\n");
        return 0;
      }
      else
      {
        if ((numpieces < 2) || (dependecia < 0))
        {
          exit(1);
        }
        else
        {
          
        }

      }

    }

    numpieces++;
    tabela = malloc((sizeof(Node)) * numpieces);
    for (i = 0; i < numpieces; i++)
    {
      tabela[i].posicao = i;
      tabela[i].numfilhos = 0;
      tabela[i].nivel = 0;
      tabela[i].nodulos = 0;
    }

    for (j = 0; j < dependecia; j++)
    {
      if (2 == 2)
      {
        pessa = new_sym_var(sizeof(int) * 8);
        dependente = new_sym_var(sizeof(int) * 8);
        colocaposicao(dependente, pessa);
      }
      else
      {
        
      }

    }

    vetor = malloc((sizeof(Node_Aux)) * numpieces);
    for (k = 0; k < numpieces; k++)
    {
      vetor[k].valor = k;
      vetor[k].numpais = 0;
    }

    for (l = 1; l < numpieces; l++)
    {
      if (tabela[l].numfilhos > 0)
      {
        checkpais(l);
      }
      else
      {
        
      }

    }

    if (dependecia == 0)
    {
      min_interacao = numpieces;
    }
    else
    {
      if (dependecia == 1)
      {
        min_interacao = numpieces - 1;
      }
      else
      {
        interacoes();
      }

    }

    if (dependecia == 0)
    {
      maior_caminho = 0;
    }
    else
    {
      if (dependecia == 1)
      {
        maior_caminho = 1;
      }
      else
      {
        for (m = 1; m < numpieces; m++)
        {
          if (vetor[m].numpais == 0)
          {
            tabela[m].nivel = 1;
            caminho(m);
          }
          else
          {
            
          }

        }

      }

    }

    printf("%d %d\n", min_interacao, maior_caminho);
    free(tabela);
    free(vetor);
    return 0;
  }
  else
  {
    return 1;
  }

}

