/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "hash_equipas.h"


Equipa **ENova_Hash(int max)
{
  Equipa **Hash;
  int i;
  Hash = (Equipa **) malloc(max * (sizeof(Equipa *)));
  for (i = 0; i < max; i++)
    Hash[i] = 0;

  return Hash;
}

Equipa **Eexpande(Equipa **Hash, int M)
{
  int i;
  Equipa **aux = Hash;
  Hash = ENova_Hash(M);
  for (i = 0; i < (M / 2); i++)
    if (aux[i] != 0)
  {
    EInsere_Hash(Hash, aux[i], M);
  }
  else
  {
    
  }


  free(aux);
  return Hash;
}

void EInsere_Hash(Equipa **Hash, Equipa *E, int M)
{
  Chave x = E->nome;
  int i = hash(x, M);
  while (Hash[i] != 0)
    i = (i + 1) % M;

  Hash[i] = E;
}

Equipa *EProcura_Hash(Equipa **Hash, Chave x, int M)
{
  int i = hash(x, M);
  while (Hash[i] != 0)
  {
    if (strcmp(x, Hash[i]->nome) == 0)
    {
      return Hash[i];
    }
    else
    {
      i = (i + 1) % M;
    }

  }

  return 0;
}

void EDestroi_Hash(Equipa **Hash, Equipas *Lst)
{
  Equipa *aux;
  Equipa *E = Lst->head;
  while (E != 0)
  {
    aux = E->next;
    elimina_equipa(E);
    E = aux;
  }

  free(Hash);
}

