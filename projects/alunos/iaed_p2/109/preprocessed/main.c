#include "/home/fmarques/sbugs/projects/alunos/lib/stubs.h"
/*File generated by PreProcessor.py*/


#include "allfunctions.h"


int main()
{
  int cmd;
  int NL = 1;
  HashTeam hashteams = tHashInit(769);
  HashGame hashgames = gHashInit(769);
  GameLink allgames = 0;
  while ((cmd = getchar()) != EOF)
  {
    switch (cmd)
    {
      case 'A':
        _addTeam_(&hashteams, NL);
        NL++;
        break;

      case 'P':
        _searchTeam_(hashteams, NL);
        NL++;
        break;

      case 'a':
        _addGame_(&hashgames, &allgames, hashteams, NL);
        NL++;
        break;

      case 'p':
        _searchGame_(hashgames, NL);
        NL++;
        break;

      case 'r':
        _deleteGame_(hashgames, &allgames, NL);
        NL++;
        break;

      case 'l':
        _listGames_(allgames, NL);
        NL++;
        break;

      case 's':
        _changeScore_(hashgames, NL);
        NL++;
        break;

      case 'g':
        _winningTeams_(hashteams, NL);
        NL++;
        break;

      case 'x':
        tHashFree(hashteams);
        gHashFree(hashgames);
        gListFree(allgames);
        break;

    }

  }

  return 0;
}

