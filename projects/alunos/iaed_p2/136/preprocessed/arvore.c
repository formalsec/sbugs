#include "/home/fmarques/sbugs/projects/alunos/lib/stubs.h"
/*File generated by PreProcessor.py*/


#include "arvore.h"
#include <stdlib.h>
#include <string.h>


arvore *ARVinit(void *e)
{
  arvore *raiz;
  raiz = malloc(sizeof(arvore));
  raiz->elem = e;
  raiz->drt = (raiz->esq = 0);
  return raiz;
}

void ARVclear(arvore *raiz)
{
  if (!raiz)
  {
    return;
  }
  else
  {
    
  }

  ARVclear(raiz->esq);
  ARVclear(raiz->drt);
  free(raiz);
}

arvore *ARVadd(arvore *raiz, void *e)
{
  int elm_comp;
  if (!raiz)
  {
    return ARVinit(e);
  }
  else
  {
    
  }

  elm_comp = strcmp(((equipa) raiz->elem)->nome, ((equipa) e)->nome);
  if (elm_comp > 0)
  {
    raiz->esq = ARVadd(raiz->esq, e);
  }
  else
  {
    if (elm_comp < 0)
    {
      raiz->drt = ARVadd(raiz->drt, e);
    }
    else
    {
      
    }

  }

  return raiz;
}

void ARVprint(arvore *raiz, int nL)
{
  if (!raiz)
  {
    return;
  }
  else
  {
    
  }

  ARVprint(raiz->esq, nL);
  escreve_equipa_g(nL, ((equipa) raiz->elem)->nome);
  ARVprint(raiz->drt, nL);
}

