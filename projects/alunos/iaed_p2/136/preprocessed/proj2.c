#include "/home/fmarques/sbugs/projects/alunos/lib/stubs.h"
/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <stdlib.h>
#include "backoffice.h"


int main()
{
  struct str_list teamHash[9973];
  fila equipas = teamHash;
  struct str_list gameHash[9973];
  fila jogos = gameHash;
  struct str_list jogosOrd[1];
  fila jogos_ord = jogosOrd;
  struct str_list equipasOrd[1];
  fila equipas_ord = equipasOrd;
  int nL = 0;
  int flag = 2;
  inicializa(equipas, jogos, jogos_ord, equipas_ord);
  while (flag == 2)
  {
    int comando = getchar();
    nL++;
    switch (comando)
    {
      case 'a':
        executa_a(nL, jogos, equipas, jogos_ord);
        break;

      case 'A':
        executa_A(nL, equipas, equipas_ord);
        break;

      case 'l':
        executa_l(nL, jogos_ord);
        break;

      case 'p':
        executa_p(nL, jogos);
        break;

      case 'P':
        executa_P(nL, equipas);
        break;

      case 'r':
        executa_r(nL, jogos, jogos_ord);
        break;

      case 's':
        executa_s(nL, jogos);
        break;

      case 'g':
        executa_g(nL, equipas_ord);
        break;

      case 'x':
        flag = 0;

    }

    getchar();
  }

  liberta(equipas, jogos, jogos_ord, equipas_ord);
  return 0;
}

