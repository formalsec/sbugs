#include "/home/fmarques/sbugs/projects/alunos/lib/stubs.h"
/*File generated by PreProcessor.py*/


#include "hash.h"


unsigned int hash_jogo(char *name)
{
  int comp = strlen(name);
  int i;
  int indice = 0;
  for (i = 0; i < comp; i++)
  {
    indice += name[i];
    indice = (indice * name[i]) % 1009;
  }

  return indice;
}

unsigned int hash_equipa(char *name)
{
  int len = strlen(name);
  int i;
  int indice = 0;
  for (i = 0; i < len; i++)
  {
    indice += name[i];
    indice = (indice * name[i]) % (1009 / 2);
  }

  return indice;
}

