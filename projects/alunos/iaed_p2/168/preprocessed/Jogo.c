#include "/home/fmarques/sbugs/projects/alunos/lib/stubs.h"
/*File generated by PreProcessor.py*/


#include "Jogo.h"


void freeJogo(Jogo *jogo)
{
  free(jogo->nome);
  free(jogo->equipa1);
  free(jogo->equipa2);
  free(jogo);
}

void printJogo(Jogo *jogo, int conta_linhas)
{
  printf("%d %s %s %s %d %d\n", conta_linhas, jogo->nome, jogo->equipa1, jogo->equipa2, jogo->score_equipa1, jogo->score_equipa2);
}

void alteraScore(Jogo *jogo, int score1, int score2)
{
  jogo->score_equipa1 = score1;
  jogo->score_equipa2 = score2;
}

Jogo *novoJogo(char *nome, char *equipa1, char *equipa2, int score1, int score2, int linha_n)
{
  Jogo *novo;
  novo = (Jogo *) malloc(sizeof(Jogo));
  novo->nome = strdup(nome);
  novo->equipa1 = strdup(equipa1);
  novo->equipa2 = strdup(equipa2);
  novo->score_equipa1 = score1;
  novo->score_equipa2 = score2;
  novo->linha_n = linha_n;
  novo->next = 0;
  return novo;
}

