/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "equipas.h"
#include "jogos.h"
#include "auxiliares.h"
#include "comandos.h"


int main()
{
  char c[2];
  linkE head;
  linkJ headj;
  hptr h;
  head = 0;
  headj = 0;
  c[1] = '\0';
  aumenta(&h, 3);
  c[0] = new_sym_var(sizeof(char) * 8);
  while (c[0] != EOF)
  {
    switch (c[0])
    {
      case 'A':
        aumenta(&h, 4);
        A(&head, &h);
        break;

      case 'p':
        aumenta(&h, 4);
        p(&headj, &h);
        break;

      case 'a':
        aumenta(&h, 4);
        a(&headj, head, &h);
        break;

      case 'l':
        aumenta(&h, 4);
        l(headj, &h);
        break;

      case 'r':
        aumenta(&h, 4);
        r(&headj, &h, head);
        break;

      case 's':
        aumenta(&h, 4);
        s(&headj, &h, head);
        break;

      case 'P':
        aumenta(&h, 4);
        P(head, &h);
        break;

      case 'g':
        aumenta(&h, 4);
        g(head, &h);
        break;

      case 'x':
        freeE(&head);
        freeJ(&headj);
        exit(0);

    }

    c[0] = new_sym_var(sizeof(char) * 8);
  }

  return 0;
}

