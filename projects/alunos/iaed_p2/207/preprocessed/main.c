#include "/home/fmarques/sbugs/projects/alunos/lib/stubs.h"
/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include "funcoes.h"


int main()
{
  int exe;
  int i;
  PRINCIPAL p;
  p = malloc(sizeof(struct principal));
  p = init_stPrincipal(p);
  exe = get_comando(p);
  while (exe == 1)
  {
    exe = get_comando(p);
  }

  freeList(p->lista_jogos);
  for (i = 0; i < p->cap_jogos; i++)
  {
    if (p->hashJogo[i] != 0)
    {
      free(p->hashJogo[i]->nome);
      free(p->hashJogo[i]->equipa1);
      free(p->hashJogo[i]->equipa2);
      free(p->hashJogo[i]);
      p->hashJogo[i] = 0;
    }
    else
    {
      
    }

  }

  free(p->hashJogo);
  p->hashJogo = 0;
  for (i = 0; i < p->cap_equipas; i++)
  {
    if (p->hashEquipa[i] != 0)
    {
      free(p->hashEquipa[i]->nome);
      free(p->hashEquipa[i]);
      p->hashEquipa[i] = 0;
    }
    else
    {
      
    }

  }

  free(p->hashEquipa);
  free(p->lista_equipas);
  free(p);
  return 0;
}

