#include "/home/fmarques/sbugs/projects/alunos/lib/stubs.h"
/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "game.h"


void freeGame(Game game)
{
  free(game->name);
  free(game);
}

void printGame(Game game, int c)
{
  printf("%d %s %s %s %d %d\n", c, game->name, game->team1->name, game->team2->name, game->score1, game->score2);
}

Game newGame(char *name, Team team1, Team team2, int score1, int score2)
{
  Game new = (Game) malloc(sizeof(struct game_node));
  new->name = malloc((sizeof(char)) * (strlen(name) + 1));
  strcpy(new->name, name);
  new->team1 = team1;
  new->team2 = team2;
  new->score1 = score1;
  new->score2 = score2;
  return new;
}

Team winner(Game game)
{
  if (game->score1 > game->score2)
  {
    return game->team1;
  }
  else
  {
    if (game->score2 > game->score1)
    {
      return game->team2;
    }
    else
    {
      
    }

  }

  return 0;
}

void changeScore(Game game, int nScore1, int nScore2)
{
  Team win2;
  Team win1 = winner(game);
  game->score1 = nScore1;
  game->score2 = nScore2;
  win2 = winner(game);
  if (win1)
  {
    rm_win(win1);
  }
  else
  {
    
  }

  if (win2)
  {
    add_win(win2);
  }
  else
  {
    
  }

}

