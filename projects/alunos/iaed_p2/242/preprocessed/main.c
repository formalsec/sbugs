/*File generated by PreProcessor.py*/


#include <stdio.h>    
#include <stdlib.h>
#include <string.h>
#include "structures.h"
#include "team_functions.h"
#include "game_functions.h"


int main()
{
  int bol = 1;
  char name[1024];
  char t1[1024];
  char t2[1024];
  int g_t1;
  int g_t2;
  int NL = 1;
  team *hash_table_team[1997];
  game *hash_table_game[1997];
  team *max_teams;
  nodeGame *game_order;
  init_hash_table_team(hash_table_team);
  init_hash_table_game(hash_table_game);
  game_order = 0;
  max_teams = 0;
  while (bol)
  {
    char cmd = getchar();
    switch (cmd)
    {
      case 'a':
        getchar();
        for (int name_index = 0; name_index < 10; name_index++)
      {
        name[name_index] = new_sym_var(sizeof(char) * 8);
      }

        name[10 - 1] = '\0';
        for (int t1_index = 0; t1_index < 10; t1_index++)
      {
        t1[t1_index] = new_sym_var(sizeof(char) * 8);
      }

        t1[10 - 1] = '\0';
        for (int t2_index = 0; t2_index < 10; t2_index++)
      {
        t2[t2_index] = new_sym_var(sizeof(char) * 8);
      }

        t2[10 - 1] = '\0';
        g_t1 = new_sym_var(sizeof(int) * 8);
        g_t2 = new_sym_var(sizeof(int) * 8);
        cmd_a(name, t1, t2, g_t1, g_t2, NL, hash_table_game, hash_table_team, &game_order);
        NL++;
        break;

      case 'A':
        getchar();
        for (int name_index = 0; name_index < 10; name_index++)
      {
        name[name_index] = new_sym_var(sizeof(char) * 8);
      }

        name[10 - 1] = '\0';
        cmd_A(name, NL, hash_table_team);
        NL++;
        break;

      case 'p':
        getchar();
        for (int name_index = 0; name_index < 10; name_index++)
      {
        name[name_index] = new_sym_var(sizeof(char) * 8);
      }

        name[10 - 1] = '\0';
        cmd_p(name, NL, hash_table_game);
        NL++;
        break;

      case 'P':
        getchar();
        for (int name_index = 0; name_index < 10; name_index++)
      {
        name[name_index] = new_sym_var(sizeof(char) * 8);
      }

        name[10 - 1] = '\0';
        cmd_P(name, NL, hash_table_team);
        NL++;
        break;

      case 'l':
        reverse(&game_order);
        cmd_l(game_order, NL);
        reverse(&game_order);
        NL++;
        break;

      case 's':
        getchar();
        for (int name_index = 0; name_index < 10; name_index++)
      {
        name[name_index] = new_sym_var(sizeof(char) * 8);
      }

        name[10 - 1] = '\0';
        g_t1 = new_sym_var(sizeof(int) * 8);
        g_t2 = new_sym_var(sizeof(int) * 8);
        cmd_s(hash_table_team, hash_table_game, name, g_t1, g_t2, NL);
        NL++;
        break;

      case 'r':
        getchar();
        for (int name_index = 0; name_index < 10; name_index++)
      {
        name[name_index] = new_sym_var(sizeof(char) * 8);
      }

        name[10 - 1] = '\0';
        cmd_r(hash_table_team, hash_table_game, name, NL, &game_order);
        NL++;
        break;

      case 'g':
        getchar();
        cmd_g(&max_teams, NL, hash_table_team);
        NL++;
        break;

      case 'x':
        bol = 0;
        destroy_games(hash_table_game);
        destroy_teams(hash_table_team);
        destroy_order(&game_order);
        break;

    }

  }

  return 0;
}

