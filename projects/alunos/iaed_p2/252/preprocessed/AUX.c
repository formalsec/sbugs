#include "/home/fmarques/sbugs/projects/alunos/lib/stubs.h"
/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "AUX.h"


team_list *AlocaEquipas()
{
  int i;
  team_list *lista = malloc(sizeof(team_list));
  lista->head = 0;
  lista->last = 0;
  lista->hashtable = malloc((sizeof(Equipa)) * 100109);
  for (i = 0; i < 100109; i++)
  {
    *(lista->hashtable + i) = 0;
  }

  return lista;
}

game_list *AlocaJogos()
{
  int i;
  game_list *lista = malloc(sizeof(game_list));
  lista->head = 0;
  lista->last = 0;
  lista->hashtable = malloc((sizeof(Jogo)) * 100109);
  for (i = 0; i < 100109; i++)
  {
    *(lista->hashtable + i) = 0;
  }

  return lista;
}

void freeList(team_list *l)
{
  Equipa *next_team;
  while (l->head != 0)
  {
    next_team = l->head->next;
    free(l->head->nome);
    free(l->head);
    l->head = next_team;
  }

  free(l->hashtable);
  free(l);
  return;
}

void freeGameList(game_list *l)
{
  Jogo *next_team;
  while (l->head != 0)
  {
    next_team = l->head->next;
    free(l->head->nome);
    free(l->head->equipa1);
    free(l->head->equipa2);
    free(l->head);
    l->head = next_team;
  }

  free(l->hashtable);
  free(l);
  return;
}

