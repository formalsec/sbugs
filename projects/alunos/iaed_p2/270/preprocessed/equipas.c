/*File generated by PreProcessor.py*/


#include "equipas.h"
#include "tabdispercao.h"


void liberta_equipa(pEquipa a)
{
  free(a->nome);
  free(a);
}

void imprime_equipa(pEquipa a)
{
  printf("%s %d\n", a->nome, a->jogos_ganhos);
}

pEquipa nova_equipa(char *nome)
{
  pEquipa eq_aux = malloc(sizeof(struct equipa));
  eq_aux->nome = strdup(nome);
  eq_aux->jogos_ganhos = 0;
  return eq_aux;
}

