/*File generated by PreProcessor.py*/


#include "listadligada.h"
#include <stdlib.h>
#include <string.h>


pLista cria_lista()
{
  pLista l = malloc(sizeof(struct lista));
  l->primeiro = (l->ultimo = 0);
  return l;
}

void liberta_lista(pLista l)
{
  free(l);
}

void rm_no(pLista l, pJogo n)
{
  if (n->anterior == 0)
  {
    l->primeiro = n->seguinte;
  }
  else
  {
    n->anterior->seguinte = n->seguinte;
  }

  if (n->seguinte == 0)
  {
    l->ultimo = n->anterior;
  }
  else
  {
    n->seguinte->anterior = n->anterior;
  }

}

void adiciona_fim(pLista l, pJogo n)
{
  n->anterior = l->ultimo;
  n->seguinte = 0;
  if (l->ultimo)
  {
    l->ultimo->seguinte = n;
  }
  else
  {
    l->primeiro = n;
  }

  l->ultimo = n;
}

