#include "/home/fmarques/sbugs/projects/alunos/lib/stubs.h"
/*File generated by PreProcessor.py*/


#include "comandos.h"


int main()
{
  pTabEquipas ht_eq = inicia_te(6603);
  pTabJogos ht_jg = inicia_tj(20603);
  pLista ls_jg = cria_lista();
  int nl = 0;
  while (1)
  {
    char comando;
    comando = getchar();
    nl++;
    switch (comando)
    {
      case 'a':
        cmd_a(nl, ls_jg, ht_eq, ht_jg);
        break;

      case 'A':
        cmd_A(nl, ht_eq);
        break;

      case 'l':
        cmd_l(nl, ls_jg);
        break;

      case 'p':
        cmd_p(nl, ht_jg);
        break;

      case 'P':
        cmd_P(nl, ht_eq);
        break;

      case 'r':
        cmd_r(nl, ls_jg, ht_jg);
        break;

      case 's':
        cmd_s(nl, ht_jg);
        break;

      case 'g':
        cmd_g(nl, ht_eq);
        break;

      case 'x':
        liberta_tudo(ls_jg, ht_eq, ht_jg);
        return 0;

    }

    getchar();
  }

}

