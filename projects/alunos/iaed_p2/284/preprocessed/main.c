#include "/home/fmarques/sbugs/projects/alunos/lib/stubs.h"
/*File generated by PreProcessor.py*/


#include "constants.h"
#include "all.h"
#include "List.h"
#include <stdio.h>


void collisions(link **h);
int main()
{
  long int NL = 0;
  char c;
  list *game_list = create_list();
  list *team_list = create_list();
  while ((c = getchar()) != 'x')
  {
    NL++;
    switch (c)
    {
      case 'a':
        add_game(NL, game_list, team_list);
        break;

      case 'A':
        add_team(NL, team_list);
        break;

      case 'l':
        list_games(NL, game_list);
        break;

      case 'p':
        find_game(NL, game_list);
        break;

      case 'P':
        find_team(NL, team_list);
        break;

      case 'r':
        remove_game(NL, game_list);
        break;

      case 's':
        change_game(NL, game_list);
        break;

      case 'g':
        get_winners(NL, team_list);
        break;

      default:
        NL--;
        break;

    }

  }

  free_list(team_list);
  free_list(game_list);
  return 0;
}

void collisions(link **h)
{
  int count = 0;
  int i;
  int max = 0;
  link *head;
  for (i = 0; i < 30011; i++)
    if (h[i])
  {
    int j = 0;
    head = h[i]->next;
    while (head)
    {
      count++;
      j++;
      head = head->next;
    }

    if (j > max)
    {
      max = j;
    }
    else
    {
      
    }

  }
  else
  {
    
  }


  printf("%d - %d\n", count, max);
}

