#include "/home/fmarques/sbugs/projects/alunos/lib/stubs.h"
/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <stdlib.h>
#include "sistema.h"


int main(void)
{
  int cmd = EOF;
  size_t count = 0;
  sistema *s = inic_sistema();
  do
  {
    switch (cmd = rd_cmd())
    {
      case 'a':
        cmd_a(++count, s);
        rd_ws();
        break;

      case 'A':
        cmd_A(++count, s);
        rd_ws();
        break;

      case 'p':
        cmd_p(++count, s);
        rd_ws();
        break;

      case 'P':
        cmd_P(++count, s);
        rd_ws();
        break;

      case 's':
        cmd_s(++count, s);
        rd_ws();
        break;

      case 'l':
        cmd_l(++count, s);
        break;

      case 'r':
        cmd_r(++count, s);
        rd_ws();
        break;

      case 'g':
        cmd_g(++count, s);
        break;

    }

  }
  while (cmd != 'x');
  free_sistema(s);
  return 0;
}

