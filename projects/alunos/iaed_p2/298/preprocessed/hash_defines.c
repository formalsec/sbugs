/*File generated by PreProcessor.py*/


#include "hash_defines.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>


unsigned long hash(char *nome)
{
  int i;
  int len = strlen(nome);
  unsigned long hash_value = 0;
  for (i = 0; i < len; i++)
  {
    hash_value += nome[i];
  }

  hash_value = hash_value % 2001;
  return hash_value;
}

