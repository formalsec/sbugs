/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "heads.h"


int main(void)
{
  char comando = ' ';
  int NL = 1;
  int HT_tam = 1889;
  list_jogos *jogos = mk_list();
  list_equipas *teams = mk_list_eq();
  HT_jogos *HT_jogos = makeHTJ(HT_tam);
  HT_equipas *HT_equipas = makeHTE(HT_tam);
  while (comando != 'x')
  {
    comando = new_sym_var(sizeof(char) * 8);
    if (comando == 'a')
    {
      add_jovo(NL, jogos, HT_jogos, HT_equipas, HT_tam);
      NL = NL + 1;
    }
    else
    {
      
    }

    if (comando == 'l')
    {
      lista_jogos(NL, jogos);
      NL = NL + 1;
    }
    else
    {
      
    }

    if (comando == 'p')
    {
      procura_jogo(NL, HT_jogos, HT_tam);
      NL = NL + 1;
    }
    else
    {
      
    }

    if (comando == 'r')
    {
      apaga_jogo(NL, jogos, HT_jogos, HT_equipas, HT_tam);
      NL = NL + 1;
    }
    else
    {
      
    }

    if (comando == 's')
    {
      altera_score(NL, HT_jogos, HT_equipas, HT_tam);
      NL = NL + 1;
    }
    else
    {
      
    }

    if (comando == 'A')
    {
      add_equipa(NL, teams, HT_equipas, HT_tam);
      NL = NL + 1;
    }
    else
    {
      
    }

    if (comando == 'P')
    {
      procura_equipa(NL, HT_equipas, HT_tam);
      NL = NL + 1;
    }
    else
    {
      
    }

    if (comando == 'g')
    {
      encontra_equipas(NL, teams);
      NL = NL + 1;
    }
    else
    {
      
    }

  }

  freeHTE(HT_equipas, HT_tam);
  freeHTJ(HT_jogos, HT_tam);
  free_list_equipas(teams);
  free_list_jogos(jogos);
  return 0;
}

