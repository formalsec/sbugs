#include "/home/fmarques/sbugs/projects/alunos/lib/stubs.h"
/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "hash_equipa.h"


linkE *InitEquipa(int M)
{
  int i;
  linkE *heads;
  heads = (linkE *) malloc(M * (sizeof(linkE)));
  for (i = 0; i < M; i++)
    heads[i] = 0;

  return heads;
}

linkE InsertEquipa(char *nome, linkE head)
{
  linkE new = (linkE) malloc(sizeof(struct nodeE));
  new->equipa = newEquipa(nome);
  new->next = head;
  return new;
}

pEquipa SearchEquipa(char *nome, linkE head)
{
  linkE x;
  for (x = head; x != 0; x = x->next)
  {
    if (strcmp(x->equipa->nome, nome) == 0)
    {
      return x->equipa;
    }
    else
    {
      
    }

  }

  return 0;
}

void FreeHashEquipa(int M, linkE *heads)
{
  linkE x;
  linkE prev;
  int i;
  for (i = 0; i < M; i++)
  {
    x = heads[i];
    while (x != 0)
    {
      prev = x;
      x = x->next;
      freeEquipa(prev->equipa);
      free(prev);
    }

  }

  free(x);
}

