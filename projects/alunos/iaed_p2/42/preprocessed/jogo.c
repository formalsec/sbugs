#include "/home/fmarques/sbugs/projects/alunos/lib/stubs.h"
/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "jogo.h"


void freeJogo(pJogo j)
{
  free(j->nome);
  free(j->equipa1);
  free(j->equipa2);
  free(j);
}

void printJogo(pJogo j)
{
  printf("%s %s %s %d %d\n", j->nome, j->equipa1, j->equipa2, j->score1, j->score2);
}

pJogo newJogo(char *nome, char *equipa1, char *equipa2, int score1, int score2)
{
  pJogo new = (pJogo) malloc(sizeof(struct jogo));
  new->nome = (char *) malloc((sizeof(char)) * (strlen(nome) + 1));
  strcpy(new->nome, nome);
  new->equipa1 = (char *) malloc((sizeof(char)) * (strlen(equipa1) + 1));
  strcpy(new->equipa1, equipa1);
  new->equipa2 = (char *) malloc((sizeof(char)) * (strlen(equipa2) + 1));
  strcpy(new->equipa2, equipa2);
  new->score1 = score1;
  new->score2 = score2;
  return new;
}

