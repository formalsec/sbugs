#include "/home/fmarques/sbugs/projects/alunos/lib/stubs.h"
/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "Game.h"
#include "Team.h"


GAME newGame(char *name, TEAM team1, TEAM team2, int home, int away)
{
  GAME g = malloc(sizeof(struct Game));
  g->Gamename = malloc((sizeof(char)) * (strlen(name) + 1));
  strcpy(g->Gamename, name);
  g->Team1 = team1;
  g->Team2 = team2;
  g->scoreHome = home;
  g->scoreAway = away;
  return g;
}

void deleteGame(GAME i)
{
  if (i)
  {
    free(i->Gamename);
    free(i);
  }
  else
  {
    
  }

}

void seeGame(GAME i)
{
  if (i)
  {
    printf("%s %s %s %d %d\n", i->Gamename, i->Team1->Teamname, i->Team2->Teamname, i->scoreHome, i->scoreAway);
  }
  else
  {
    
  }

}

TEAM winner(GAME i)
{
  if (i->scoreHome > i->scoreAway)
  {
    return i->Team1;
  }
  else
  {
    if (i->scoreHome < i->scoreAway)
    {
      return i->Team2;
    }
    else
    {
      return 0;
    }

  }

}

