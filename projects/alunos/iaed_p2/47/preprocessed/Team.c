#include "/home/fmarques/sbugs/projects/alunos/lib/stubs.h"
/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "Team.h"


TEAM newTeam(char *name)
{
  TEAM g = malloc(sizeof(struct Team));
  g->Teamname = malloc((sizeof(char)) * (strlen(name) + 1));
  strcpy(g->Teamname, name);
  g->wongames = 0;
  return g;
}

void deleteTeam(TEAM i)
{
  if (i)
  {
    free(i->Teamname);
    free(i);
  }
  else
  {
    
  }

}

void seeTeam(TEAM i)
{
  if (i)
  {
    printf("%s", i->Teamname);
  }
  else
  {
    
  }

}

