/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "list.h"


link create(char *pbuffer[], int score1, int score2, int id)
{
  link match = (link) malloc(sizeof(struct node));
  match->name = (char *) malloc(((sizeof(char)) * strlen(pbuffer[0])) + 1);
  strcpy(match->name, pbuffer[0]);
  match->team1 = (char *) malloc(((sizeof(char)) * strlen(pbuffer[1])) + 1);
  strcpy(match->team1, pbuffer[1]);
  match->team2 = (char *) malloc(((sizeof(char)) * strlen(pbuffer[2])) + 1);
  strcpy(match->team2, pbuffer[2]);
  match->score1 = score1;
  match->score2 = score2;
  match->id = id;
  match->next = 0;
  return match;
}

link insert(char *pbuffer[], int score1, int score2, int id, link head)
{
  link match;
  if (head == 0)
  {
    return create(pbuffer, score1, score2, id);
  }
  else
  {
    
  }

  for (match = head; match->next != 0; match = match->next)
    ;

  match->next = create(pbuffer, score1, score2, id);
  return head;
}

void print(int nl, link head)
{
  link new_head;
  for (new_head = head; new_head != 0; new_head = head->next)
    printf("%d %s %s %s %d %d\n", nl, new_head->name, new_head->team1, new_head->team2, new_head->score1, new_head->score2);

}

