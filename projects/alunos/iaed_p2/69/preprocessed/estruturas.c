#include "/home/fmarques/sbugs/projects/alunos/lib/stubs.h"
/*File generated by PreProcessor.py*/


#include "estruturas.h"
#include <string.h>


int cria_id(char *nome)
{
  int tamanho;
  int id = 0;
  int i = 0;
  tamanho = strlen(nome);
  for (i = 0; i <= tamanho; i++)
    id += nome[i];

  return id;
}

Jogo novo_jogo(char *nome1, char *equipa_1, char *equipa_2, int score1, int score2)
{
  int tamanho;
  Jogo novo;
  novo = (Jogo) malloc(sizeof(struct str_jogo));
  tamanho = strlen(nome1) + 1;
  novo->nome_jogo = (char *) malloc((sizeof(char)) * tamanho);
  strcpy(novo->nome_jogo, nome1);
  tamanho = strlen(equipa_1) + 1;
  novo->equipa1 = (char *) malloc((sizeof(char)) * tamanho);
  strcpy(novo->equipa1, equipa_1);
  novo->equipa2 = (char *) malloc((sizeof(char)) * (strlen(equipa_2) + 1));
  strcpy(novo->equipa2, equipa_2);
  novo->sc1 = score1;
  novo->sc2 = score2;
  novo->id = cria_id(nome1);
  return novo;
}

Equipa nova_equipa(char *nome_equipa)
{
  Equipa nova;
  nova = (Equipa) malloc(sizeof(struct str_equipa));
  nova->nome = (char *) malloc((sizeof(char)) * (strlen(nome_equipa) + 1));
  strcpy(nova->nome, nome_equipa);
  nova->vitorias = 0;
  nova->id = cria_id(nome_equipa);
  return nova;
}

void free_jogo(Jogo a)
{
  free(a->nome_jogo);
  free(a->equipa1);
  free(a->equipa2);
  free(a);
}

void free_equipa(Equipa a)
{
  free(a->nome);
  free(a);
}

