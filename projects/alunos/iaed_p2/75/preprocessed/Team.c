#include "/home/fmarques/sbugs/projects/alunos/lib/stubs.h"
/*File generated by PreProcessor.py*/


#include <stdlib.h>
#include <string.h>
#include "Team.h"


Team *newTeam(char *name)
{
  Team *ptr_team = (Team *) malloc(sizeof(Team));
  ptr_team->name = (char *) malloc((sizeof(char)) * (strlen(name) + 1));
  strcpy(ptr_team->name, name);
  ptr_team->victories = 0;
  return ptr_team;
}

void deleteTeam(Team *tm)
{
  free(tm->name);
  free(tm);
}

void change_victory(Team *pteam, int change)
{
  if (change == 1)
  {
    pteam->victories++;
  }
  else
  {
    
  }

  if (change == (-1))
  {
    pteam->victories--;
  }
  else
  {
    
  }

}

