/*File generated by PreProcessor.py*/


#include "hash.h"
#include <stdio.h>


int main()
{
  pListas *listas = {0};
  pLst_Ord lista_ordenada = {0};
  char c;
  int NL = 0;
  listas = inicializa_listas(listas, 1777);
  lista_ordenada = inicializa_lista_ordenada(lista_ordenada);
  while ((c = getchar()) != 'x')
  {
    switch (c)
    {
      case 'A':
        NL++;
        adiciona_equipa(NL, listas, lista_ordenada, 1777);
        break;

      case 'P':
        NL++;
        consulta_equipa(NL, listas, 1777);
        break;

      case 'a':
        NL++;
        adiciona_jogo(NL, listas, lista_ordenada, 1777);
        break;

      case 'p':
        NL++;
        consulta_jogo(NL, listas, 1777);
        break;

      case 'l':
        NL++;
        jogos_ordenados(NL, lista_ordenada);
        break;

      case 'r':
        NL++;
        remove_jogo(NL, listas, lista_ordenada, 1777);
        break;

      case 's':
        NL++;
        altera_pontuacao(NL, listas, 1777);
        break;

      case 'g':
        NL++;
        ordena_equipas_vitorias(NL, lista_ordenada);
        break;

    }

    while (c != '\n')
      c = getchar();

  }

  free_listas(listas, 1777);
  free_lista_ordenada(lista_ordenada);
  return 0;
}

