#include "/home/fmarques/sbugs/projects/alunos/lib/stubs.h"
/*File generated by PreProcessor.py*/


#include "List_T2.h"


LTlink LTinit()
{
  return 0;
}

LTlink LTnew(Team team)
{
  LTlink new = (LTlink) malloc(sizeof(struct LTnode));
  new->team = team;
  new->next = 0;
  return new;
}

LTlink LTinsert(LTlink LThead, Team team)
{
  LTlink x;
  LTlink prev;
  if (LThead == 0)
  {
    x = LTnew(team);
    return x;
  }
  else
  {
    for (x = LThead, prev = 0; (x != 0) && Tless(team, LTgetteam(x)); prev = x, x = x->next)
      ;

    if (x == LThead)
    {
      x = LTnew(team);
      x->next = LThead;
      return x;
    }
    else
    {
      x = LTnew(team);
      x->next = prev->next;
      prev->next = x;
    }

  }

  return LThead;
}

LTlink LTdelete(LTlink LThead, Team team)
{
  LTlink x;
  LTlink prev;
  for (x = LThead, prev = 0; x != 0; prev = x, x = x->next)
  {
    if (strcmp(Tgetname(team), Tgetname(LTgetteam(x))) == 0)
    {
      if (x == LThead)
      {
        LThead = x->next;
      }
      else
      {
        prev->next = x->next;
      }

      free(x);
      break;
    }
    else
    {
      
    }

  }

  return LThead;
}

LTlink LTclear(LTlink LThead)
{
  Team team;
  while (LThead != 0)
  {
    team = LTgetteam(LThead);
    LThead = LTdelete(LThead, team);
  }

  return 0;
}

void LTprint(LTlink LThead, int NL)
{
  LTlink x;
  int max;
  if (LThead != 0)
  {
    x = LThead;
    max = Tgetwins(LTgetteam(x));
    printf("%d Melhores %d\n", NL, max);
    for (; (x != 0) && (Tgetwins(LTgetteam(x)) == max); x = x->next)
      printf("%d * %s\n", NL, Tgetname(LTgetteam(x)));

  }
  else
  {
    
  }

}

Team LTgetteam(LTlink link)
{
  return link->team;
}

