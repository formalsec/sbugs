#ifndef _STRDUP_
#define _STRDUP_

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/* Allocs memory for a string. Returns a pointer to that memory */
char *my_strdup(char *source);

#endif