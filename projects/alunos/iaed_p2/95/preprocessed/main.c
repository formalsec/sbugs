/*File generated by PreProcessor.py*/


#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include "geral.h"
#include "structs.h"


int main()
{
  int comando;
  int cont = 1;
  table_eq tab_eq;
  table_jogos tab_j;
  listagem lst;
  lst = list();
  tab_eq = Init_eq(1001);
  tab_j = Init_jogo(1001);
  while ((comando = getchar()) != EOF)
  {
    switch (comando)
    {
      case 'a':
        a(cont, tab_eq, tab_j, lst);
        break;

      case 'l':
        l(cont, lst);
        break;

      case 'p':
        p(cont, tab_j);
        break;

      case 'r':
        r(cont, tab_j, tab_eq, lst);
        break;

      case 's':
        s(cont, tab_j, tab_eq);
        break;

      case 'A':
        A(cont, tab_eq);
        break;

      case 'P':
        P(cont, tab_eq);
        break;

      case 'g':
        g(cont, tab_eq);
        break;

      case 'x':
        liberta_hash_eq(tab_eq);
        liberta_hash_j(tab_j);
        free(lst);
        break;

    }

    cont++;
    getchar();
  }

  return 0;
}

