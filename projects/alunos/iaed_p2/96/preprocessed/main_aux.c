/*File generated by PreProcessor.py*/


#include "main_aux.h"


void guarda_lista_ord(bloco_hash_jogo *head, bloco_hash_jogo *tail, jogo *j)
{
  bloco_hash_jogo novo_el;
  novo_el = malloc(sizeof(struct bloco_h_jogo));
  novo_el->jogo = j;
  novo_el->next = 0;
  if (head[0] == 0)
  {
    head[0] = novo_el;
    tail[0] = head[0];
  }
  else
  {
    tail[0]->next = novo_el;
    tail[0] = novo_el;
  }

}

void apaga_lista_ord(bloco_hash_jogo *head_ord, bloco_hash_jogo *tail_ord, char *nome)
{
  bloco_hash_jogo aux = *head_ord;
  bloco_hash_jogo ante = 0;
  while ((aux != 0) && (strcmp(aux->jogo->nome, nome) != 0))
  {
    ante = aux;
    aux = aux->next;
  }

  if (aux == 0)
  {
    return;
  }
  else
  {
    
  }

  if (ante == 0)
  {
    *head_ord = aux->next;
  }
  else
  {
    ante->next = aux->next;
  }

  if (aux == (*tail_ord))
  {
    *tail_ord = ante;
  }
  else
  {
    
  }

  free(aux);
}

void guarda_lista_e(bloco_hash_equipa *head, equipa *e)
{
  bloco_hash_equipa novo_e;
  novo_e = malloc(sizeof(struct bloco_h_equipa));
  novo_e->equipa = e;
  novo_e->next = head[0];
  head[0] = novo_e;
}

void guarda_ord_alfabetica(bloco_hash_equipa *lista, equipa *e)
{
  bloco_hash_equipa novo_e;
  bloco_hash_equipa aux;
  novo_e = malloc(sizeof(struct bloco_h_equipa));
  novo_e->equipa = e;
  if (((*lista) == 0) || (strcmp((*lista)->equipa->nome, novo_e->equipa->nome) > 0))
  {
    novo_e->next = *lista;
    *lista = novo_e;
  }
  else
  {
    aux = *lista;
    while ((aux->next != 0) && (strcmp(aux->next->equipa->nome, e->nome) < 0))
    {
      aux = aux->next;
    }

    novo_e->next = aux->next;
    aux->next = novo_e;
  }

}

