#include "/home/fmarques/sbugs/projects/alunos/lib/stubs.h"
/*File generated by PreProcessor.py*/


#include "main_f.h"


int main()
{
  int NL = 1;
  bloco_hash_equipa *table_e;
  bloco_hash_equipa *lista_e;
  bloco_hash_jogo *table_j;
  bloco_hash_jogo *lista_j_ord_h;
  bloco_hash_jogo *lista_j_ord_t;
  lista_j_ord_h = malloc(sizeof(bloco_hash_jogo));
  lista_j_ord_t = malloc(sizeof(bloco_hash_jogo));
  lista_e = malloc(sizeof(bloco_hash_equipa));
  lista_j_ord_h[0] = 0;
  lista_j_ord_t[0] = 0;
  lista_e[0] = 0;
  table_e = inicializa_hash_e();
  table_j = inicializa_hash_j();
  while (1)
  {
    char c;
    c = getchar();
    switch (c)
    {
      case 'A':
        A(table_e, lista_e, NL);
        NL++;
        break;

      case 'a':
        a(table_j, table_e, lista_j_ord_h, lista_j_ord_t, NL);
        NL++;
        break;

      case 'P':
        P(table_e, NL);
        NL++;
        break;

      case 'p':
        p(table_j, NL);
        NL++;
        break;

      case 's':
        s(table_j, table_e, NL);
        NL++;
        break;

      case 'r':
        r(table_j, lista_j_ord_h, lista_j_ord_t, NL);
        NL++;
        break;

      case 'l':
        l(lista_j_ord_h, NL);
        NL++;
        break;

      case 'g':
        g(lista_e, NL);
        NL++;
        break;

      case 'x':
        x(table_e, lista_e, table_j, lista_j_ord_h, lista_j_ord_t);
        return 0;

    }

  }

  return 0;
}

